// ¡Vacío o solo esto!
// La configuración ya está en el build.gradle raíz

sourceSets.main.java.srcDirs = ["src/main/java/"]
sourceSets.main.resources.srcDirs = ["src/main/resources/"]

project.ext.mainClassName = "com.mygdx.game.LWJGL3Launcher"
project.ext.assetsDir = new File("../core/assets")

task run(dependsOn: classes, type: JavaExec) {
  main = project.mainClassName
  classpath = sourceSets.main.runtimeClasspath
  standardInput = System.in
  workingDir = project.assetsDir
  ignoreExitValue = true
}
task createJar(type: Jar) {
  manifest {
    attributes 'Main-Class': 'com.mygdx.game.LWJGL3Launcher'
  }
  from sourceSets.main.output
  from project(":core").sourceSets.main.output
  from { configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) } }
  duplicatesStrategy = DuplicatesStrategy.EXCLUDE
}

task runJar(dependsOn: createJar, type: JavaExec) {
  main = "-jar"
  args = ["build/libs/lwjgl3.jar"]
}
